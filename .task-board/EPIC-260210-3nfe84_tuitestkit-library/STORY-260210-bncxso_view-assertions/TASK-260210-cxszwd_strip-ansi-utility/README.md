# TASK-260210-cxszwd: strip-ansi-utility

## Description
Core StripANSI(s string) string function wrapping charmbracelet/x/ansi.Strip(). Public utility for removing all ANSI escape sequences from strings. Tests must cover: basic colors (16), 256-color palette, truecolor (24-bit RGB), bold/italic/underline, reset sequences, combined sequences, empty string, string with no ANSI, and multi-line strings with mixed ANSI.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
