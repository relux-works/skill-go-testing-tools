# TASK-260210-1t0h1w: implement-run-reducer-sequences

## Description
Implement RunReducerSequences[S,A](t, reduce, sequences) function. For each ReducerSequence: run as t.Run sub-test, apply steps sequentially accumulating state, run per-step Assert if present, run Final assert after all steps. Steps without Assert just advance the state.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
