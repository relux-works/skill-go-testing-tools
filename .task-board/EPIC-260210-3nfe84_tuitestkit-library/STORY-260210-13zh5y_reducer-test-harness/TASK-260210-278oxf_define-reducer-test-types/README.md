# TASK-260210-278oxf: define-reducer-test-types

## Description
Define core generic types: ReducerTest[S,A] struct (Name, Initial, Action, Assert), Step[S,A] struct (Name, Action, Assert), ReducerSequence[S,A] struct (Name, Initial, Steps, Final). All in reducer.go file. Assert uses func(t *testing.T, got S) signature.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
