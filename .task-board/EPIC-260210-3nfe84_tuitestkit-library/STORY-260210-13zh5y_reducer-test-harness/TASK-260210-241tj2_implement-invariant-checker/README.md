# TASK-260210-241tj2: implement-invariant-checker

## Description
Implement InvariantChecker[S] type: Invariant[S] struct (Name, Check func(S) error), NewInvariantChecker constructor, CheckAll(t, state) method that runs all invariants and reports failures with invariant name. Place in invariant.go file.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
